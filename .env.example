# Copy to .env.local and fill in values. Do not commit .env.local.

# ---- Frontend (Vite) ----
# API base URL (no trailing slash). Required in production so audit/contact work.
# Dev:  http://localhost:3001
# Prod: https://your-api-domain.com
# VITE_API_URL=http://localhost:3001

# ---- Backend (server) ----
# Optional: port (default 3001)
# PORT=3001

# Optional: frontend origin for CORS. Set in production to your site URL.
# FRONTEND_URL=https://your-site.com

# Gemini AI (for audit feature). Use VITE_GEMINI_API_KEY or GEMINI_API_KEY.
VITE_GEMINI_API_KEY=your_gemini_api_key

# Resend – full suite email (https://resend.com). Use a verified domain address for RESEND_FROM.
RESEND_API_KEY=re_xxxxxxxxxxxx
RESEND_FROM=WAX Advertising Agency <noreply@waxadvertisingagency.com>
RESEND_TO=abba@waxadvertisingagency.com
RESEND_AUTO_REPLY_ENABLED=true
