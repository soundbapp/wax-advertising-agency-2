# Production build: copy to .env.production.local and set VITE_API_URL.
# Vite reads this when running: npm run build / npm run build:prod
# Do not commit .env.production.local (add to .gitignore).

# Backend base URL (no trailing slash). Required for audit and contact in production.
# Frontend: https://www.waxadvertisingagency.com
# Backend (API): https://api.waxadvertisingagency.com
VITE_API_URL=https://api.waxadvertisingagency.com
